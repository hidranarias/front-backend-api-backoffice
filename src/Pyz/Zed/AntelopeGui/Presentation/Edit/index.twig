{% extends '@Gui/Layout/layout.twig' %}

{% set widget_title = 'Create Antelope' | trans %}

{% block action %}
    {{ backActionButton(backUrl, 'Back to antelope list' | trans) }}
{% endblock %}

{% block content %}
    {% embed '@Gui/Partials/widget.twig' %}
        {% block widget_content %}
            {{ form_start(antelopeUpdateForm) }}

            {% for field in antelopeUpdateForm.children %}
                {{ form_row(field) }}
            {% endfor %}

            <input type="submit" class="btn btn-primary safe-submit" value="{{ 'Create' | trans }}"/>

            {{ form_end(antelopeUpdateForm) }}
        {% endblock %}
    {% endembed %}
{% endblock %}
